# Phase 1 isolated POC broker only — anonymous/no-TLS is acceptable ONLY because this
# instance is not reachable outside the local Docker network and is never the production
# broker (constitution I.1; research.md #4). Do not reuse this config for anything
# production-facing.
listener 1883
allow_anonymous true
persistence false
log_dest stdout
